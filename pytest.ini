[pytest]
testpaths = tests
addopts =
    -v
    --strict-markers
    --tb=short
    --cov=.
    --cov-config=.coveragerc
    --cov-report=term-missing
markers =
    unit: fast, isolated unit tests (no external I/O)
minversion = 8.0
